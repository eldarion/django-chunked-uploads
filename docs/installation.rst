.. _installation:

Installation
============

* To install ::

    pip install django-chunked-uploads

* Add ``''`` to your ``INSTALLED_APPS`` setting::

    INSTALLED_APPS = (
        # other apps
        "chunked_uploads",
    )

